{
  "name": "bnb-guest-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Supabase-ready BnB public site, services catalog, WhatsApp ordering, admin dashboard, SEO and PWA.",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "sync-calendars": "node scripts/sync-calendars.js",
    "check": "node --check src/app.js && node --check src/lib/db.js && node --check src/lib/seo.js && node --check src/lib/whatsapp.js && node --check server.js"
  }
}
